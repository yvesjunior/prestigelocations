{
  "name": "prestige-locations",
  "private": true,
  "scripts": {
    "dev": "npm --prefix src/web run dev",
    "build": "npm --prefix src/web run build",
    "preview": "npm --prefix src/web run preview",
    "lint": "npm --prefix src/web run lint",
    "format": "npm --prefix src/web run format",
    "install:web": "npm --prefix src/web install"
  }
}
