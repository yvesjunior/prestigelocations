# Overlay dev — publie les ports sur la machine locale.
# Les ports HÔTE sont configurables via .env (utile pour lancer plusieurs sites
# sur un même serveur). Le port INTERNE des conteneurs ne change pas (3000 / 5432).
services:
  web:
    ports:
      - "${WEB_PORT:-3000}:3000"
  db:
    ports:
      - "${DB_PORT:-5432}:5432"
